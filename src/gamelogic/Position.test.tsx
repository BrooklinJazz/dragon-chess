import {Position} from "./Position"
import { A1, A2, A3, A4, A5, A6, A7, A8, B1, C1, D1, E1, F1, G1, H1 } from "../constants/positions"

test("Position .up", () => {
    const a1 = new Position(A1)
    expect(a1.up().save()).toEqual(A2)
    expect(a1.up().save()).toEqual(A3)
    expect(a1.up().save()).toEqual(A4)
    expect(a1.up().save()).toEqual(A5)
    expect(a1.up().save()).toEqual(A6)
    expect(a1.up().save()).toEqual(A7)
    expect(a1.up().save()).toEqual(A8)
    expect(a1.up().save()).toEqual(A8)
})
test("Position .down", () => {
    const a8 = new Position(A8)
    expect(a8.down().save()).toEqual(A7)
    expect(a8.down().save()).toEqual(A6)
    expect(a8.down().save()).toEqual(A5)
    expect(a8.down().save()).toEqual(A4)
    expect(a8.down().save()).toEqual(A3)
    expect(a8.down().save()).toEqual(A2)
    expect(a8.down().save()).toEqual(A1)
    expect(a8.down().save()).toEqual(A1)
})
test("Position .right", () => {
    const a1 = new Position(A1)
    expect(a1.right().save()).toEqual(B1)
    expect(a1.right().save()).toEqual(C1)
    expect(a1.right().save()).toEqual(D1)
    expect(a1.right().save()).toEqual(E1)
    expect(a1.right().save()).toEqual(F1)
    expect(a1.right().save()).toEqual(G1)
    expect(a1.right().save()).toEqual(H1)
    expect(a1.right().save()).toEqual(H1)
})
test("Position .left", () => {
    const h1 = new Position(H1)
    expect(h1.left().save()).toEqual(G1)
    expect(h1.left().save()).toEqual(F1)
    expect(h1.left().save()).toEqual(E1)
    expect(h1.left().save()).toEqual(D1)
    expect(h1.left().save()).toEqual(C1)
    expect(h1.left().save()).toEqual(B1)
    expect(h1.left().save()).toEqual(A1)
    expect(h1.left().save()).toEqual(A1)
})

test("Position .fwd black", () => {
    const a8 = new Position(A8, "black")
    expect(a8.fwd().save()).toEqual(A7)
    expect(a8.fwd().save()).toEqual(A6)
    expect(a8.fwd().save()).toEqual(A5)
    expect(a8.fwd().save()).toEqual(A4)
    expect(a8.fwd().save()).toEqual(A3)
    expect(a8.fwd().save()).toEqual(A2)
    expect(a8.fwd().save()).toEqual(A1)
    expect(a8.fwd().save()).toEqual(A1)
})